package model;

/**
 * Enum representing the side of an order in the financial messaging system.
 */
public enum OrderSide {
    BUY,  // Represents a buy order
    SELL  // Represents a sell order
}
